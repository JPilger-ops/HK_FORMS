# Base URLs
APP_URL=https://app.bistrottelegraph.de
ADMIN_BASE_URL=https://app.bistrottelegraph.de
PUBLIC_FORM_URL=https://forms.bistrottelegraph.de
NEXTAUTH_URL=https://app.bistrottelegraph.de
ENFORCE_DOMAIN_ROUTING=true
NEXTAUTH_SECRET=change-me
INVITE_LINK_HOURS=48
INVITE_TOKEN_SECRET=change-me-too
INVITE_DEFAULT_EXPIRY_DAYS=7
# Tokenpflicht ist serverseitig aktiv; dieser Schalter bleibt für Abwärtskompatibilität
INVITE_REQUIRE_TOKEN=true

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/hkforms
POSTGRES_PASSWORD=postgres
POSTGRES_USER=postgres
POSTGRES_DB=hkforms

# Pricing / Session
NEXT_PUBLIC_PRICE_PER_GUEST=35
AUTO_LOGOUT_MINUTES=30

# SMTP / Notifications
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=smtp-user
SMTP_PASS=smtp-pass
SMTP_FROM="Heidekönig Reservierungen <noreply@heidekoenig.de>"
ADMIN_NOTIFICATION_EMAILS=admin@heidekoenig.de,mitarbeiter@heidekoenig.de

# Rate limit
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=10
SEND_GUEST_CONFIRMATION=false

# Admin seeding (optional via script)
ADMIN_EMAIL=admin@heidekoenig.de
ADMIN_PASSWORD=supersecret
